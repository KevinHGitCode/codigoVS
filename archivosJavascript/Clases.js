// Este archivo contiene una serie de classes de javascript

// algunas de las clases nativas de javascript son:
// Array         https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Array
// Boolean       https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Boolean
// Date          https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Date
// Error         https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Error
// Function      https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Function
// Map           https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Map
// Number        https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Number
// Object        https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Object
// RegExp        https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/RegExp
// Set           https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Set
// String        https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/String
// Symbol        https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Symbol
// WeakMap       https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/WeakMap
// WeakSet       https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/WeakSet

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase Array
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Array
    // instancia.concat(array1, array2, ...)
    // instancia.copyWithin(target, start, end)
    // instancia.entries()
    // instancia.every(callback[, thisArg])
    // instancia.fill(value, start, end)
    // instancia.filter(callback[, thisArg])
    // instancia.find(callback[, thisArg])
    // instancia.findIndex(callback[, thisArg])
    // instancia.forEach(callback[, thisArg])
    // instancia.indexOf(searchElement[, fromIndex])
    // instancia.join(separator)
    // instancia.keys()
    // instancia.lastIndexOf(searchElement[, fromIndex])
    // instancia.map(callback[, thisArg])
    // instancia.pop()
    // instancia.push(element)
    // instancia.reduce(callback[, initialValue])
    // instancia.reduceRight(callback[, initialValue])
    // instancia.reverse()
    // instancia.shift()
    // instancia.slice(start, end)
    // instancia.some(callback[, thisArg])
    // instancia.sort(compareFunction)
    // instancia.splice(start, deleteCount[, item1[, item2[, ...]]])
    // instancia.toLocaleString()
    // instancia.toString()
    // instancia.unshift(element)
    // instancia.values()
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase Boolean
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Boolean
    // bool.toString()
    // bool.valueOf()
    // bool.toString(2)
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase Date
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Date
    // date.getDate()
    // date.getDay()
    // date.getFullYear()
    // date.getHours()
    // date.getMilliseconds()
    // date.getMinutes()
    // date.getMonth()
    // date.getSeconds()
    // date.getTime()
    // date.getTimezoneOffset()
    // date.getUTCDate()
    // date.getUTCDay()
    // date.getUTCFullYear()
    // date.getUTCHours()
    // date.getUTCMilliseconds()
    // date.getUTCMinutes()
    // date.getUTCMonth()
    // date.getUTCSeconds()
    // date.getYear()
    // date.parse(string)
    // date.setDate(date)
    // date.setFullYear(year[, month[, day]])
    // date.setHours(hours[, minutes[, seconds[, ms]]])
    // date.setMilliseconds(ms)
    // date.setMinutes(minutes[, seconds[, ms]])
    // date.setMonth(month[, day])
    // date.setSeconds(seconds[, ms])
    // date.setTime(time)
    // date.setUTCDate(date)
    // date.setUTCFullYear(year[, month[, day]])
    // date.setUTCHours(hours[, minutes[, seconds[, ms]]])
    // date.setUTCMilliseconds(ms)
    // date.setUTCMinutes(minutes[, seconds[, ms]])
    // date.setUTCMonth(month[, day])
    // date.setUTCSeconds(seconds[, ms])
    // date.setYear(year)
    // date.toDateString()
    // date.toISOString()
    // date.toJSON()
    // date.toLocaleDateString()
    // date.toLocaleString()
    // date.toLocaleTimeString()
    // date.toString()
    // date.toTimeString()
    // date.toUTCString()
    // date.valueOf()
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase Error
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Error
    // error.name
    // error.message
    // error.stack
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase Function
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Function
    // funcion.apply(thisArg, [argsArray])
    // funcion.bind(thisArg, [arg1[, arg2[, ...]]])
    // funcion.call(thisArg, arg1[, arg2[, ...]])
    // funcion.length
    // funcion.name
    // funcion.toString()
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase Map
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Map
    // map.clear()
    // map.delete(key)
    // map.entries()
    // map.forEach(callback[, thisArg])
    // map.get(key)
    // map.has(key)
    // map.keys()
    // map.set(key, value)
    // map.values()
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase Number
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Number
    // num.toExponential(fractionDigits)
    // num.toFixed(fractionDigits)
    // num.toLocaleString()
    // num.toPrecision(precision)
    // num.toString(radix)
    // num.valueOf()
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase Object
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Object
    // object.assign(target, source[, source2[, ...]])
    // object.create(proto[, propertiesObject])
    // object.defineProperty(obj, prop, descriptor)
    // object.defineProperties(obj, propertiesObject)
    // object.freeze(obj)
    // object.getOwnPropertyDescriptor(obj, prop)
    // object.getOwnPropertyDescriptors(obj)
    // object.getOwnPropertyNames(obj)
    // object.getOwnPropertySymbols(obj)
    // object.getPrototypeOf(obj)
    // object.is(value1, value2)
    // object.isExtensible(obj)
    // object.isFrozen(obj)
    // object.isSealed(obj)
    // object.keys(obj)
    // object.preventExtensions(obj)
    // object.seal(obj)
    // object.setPrototypeOf(obj, prototype)
    // object.values(obj)
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase RegExp
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/RegExp
    // regExp.compile(pattern[, flags])
    // regExp.exec(string)
    // regExp.flags
    // regExp.global
    // regExp.ignoreCase
    // regExp.lastIndex
    // regExp.match(string)
    // regExp.matchAll(string)
    // regExp.multiline
    // regExp.next()
    // regExp.prototype
    // regExp.prototype.exec(string)
    // regExp.prototype.test(string)
    // regExp.prototype.toString()
    // regExp.prototype.unicode
    // regExp.prototype.sticky
    // regExp.prototype.source
    // regExp.prototype.lastIndex
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase Set
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Set
    // set.add(value)
    // set.clear()
    // set.delete(value)
    // set.entries()
    // set.forEach(callback[, thisArg])
    // set.has(value)
    // set.keys()
    // set.values()
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase String
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/String
    // string.charAt(pos)
    // string.charCodeAt(pos)
    // string.codePointAt(pos)
    // string.concat(string1[, string2[, ...]])
    // string.endsWith(searchString[, endPosition])
    // string.fromCharCode(code1[, code2[, ...]])
    // string.fromCodePoint(code1[, code2[, ...]])
    // string.includes(searchString[, position])
    // string.indexOf(searchString[, position])
    // string.lastIndexOf(searchString[, position])
    // string.localeCompare(that)
    // string.match(regexp)
    // string.matchAll(regexp)
    // string.normalize(form)
    // string.padEnd(targetLength[, padString])
    // string.padStart(targetLength[, padString])
    // string.repeat(count)
    // string.replace(searchValue, replaceValue)
    // string.replaceAll(searchValue, replaceValue)
    // string.search(regexp)
    // string.slice(start[, end])
    // string.split(separator[, limit])
    // string.startsWith(searchString[, position])
    // string.substring(start[, end])
    // string.substr(start[, length])
    // string.toLocaleLowerCase()
    // string.toLocaleUpperCase()
    // string.toLowerCase()
    // string.toString()
    // string.toUpperCase()
    // string.trim()
    // string.trimEnd()
    // string.trimLeft()
    // string.trimRight()
    // string.trimStart()
    // string.valueOf()
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase Symbol
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/Symbol
    // symbol.for(key)
    // symbol.keyFor(symmbol)
    // symbol.prototype
    // symbol.prototype.toString()
*/

// ------------------------------------------------------------

/**
    // metodos de un instancia de la Clase WeakMap
    // https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/WeakMap
    // weakMap.delete(key)
    // weakMap.get(key)
    // weakMap.has(key)
    // weakMap.set(key, value)
*/

// ------------------------------------------------------------

/**
// metodos de un instancia de la Clase WeakSet
// https://developer.mozilla.org/es/docs/Web/JavaScript/Referencia/Objetos_globales/WeakSet
// weakSet.add(value)
// weakSet.delete(value)
// weakSet.has(value)
*/

// ------------------------------------------------------------


